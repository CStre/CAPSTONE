# Backend

Node.js + TypeScript GraphQL service for "Building Better Algorithms" — GraphQL Yoga
+ Pothos, deployed as an AWS Lambda container image.

Scaffolded in **Phase 1** of the refactor. Until then, the reference implementation
is the v1 Django API under [`../legacy/backend/`](../legacy/backend/).

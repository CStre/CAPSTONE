# react-simple-maps@3 declares a React 16/17/18 peer but works fine on React 19
# (we run it on 19). Allow the install to proceed on every npm invocation — local and
# CI `npm ci` alike — instead of relying on a manual --legacy-peer-deps flag.
legacy-peer-deps=true

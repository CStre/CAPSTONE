# GraphQL endpoint. A relative path is proxied to the backend by Vite in dev
# (see vite.config.ts) and served same-origin behind CloudFront in AWS.
VITE_GRAPHQL_URL=/graphql

# Cognito user pool — values come from the Phase 3 Terraform outputs.
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_CLIENT_ID=

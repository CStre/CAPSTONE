# Infrastructure

Terraform configuration for the AWS deployment — Lambda, DynamoDB, Cognito,
S3 + CloudFront, SSM, and the CI/CD IAM roles — across the `dev`, `qa`, and
`production` environments.

Authored in **Phase 3** of the refactor.
